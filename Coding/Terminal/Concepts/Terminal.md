# What it is:
   The **Graphical User Interface (GUI) application** that you open.
   
# Function: 
   It is the **display layer** that handles the window, keyboard input, and rendering of output. It does not interpret commands itself.
   
# Analogy:
The **phone** (the physical device/window).
