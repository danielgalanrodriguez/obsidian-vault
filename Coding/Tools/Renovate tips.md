# Config validation

When editing the configs, it's useful to be able to validate them before merging:

```zsh
brew install renovate
renovate-config-validator yourconfig.json
```

src: https://docs.renovatebot.com/config-validation/